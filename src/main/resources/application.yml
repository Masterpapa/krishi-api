##Port Configuration
server:
  port: 9443
  servlet:
    context-path: /krishi
    

spring:
  application:
    name: KRISHI
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect